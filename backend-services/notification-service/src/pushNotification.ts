// Firebase/Huawei Push Notification entegrasyonu için örnek fonksiyon
export const sendPushNotification = async (deviceToken: string, message: string) => {
  // Firebase veya Huawei Push SDK ile entegrasyon yapılır
  // Gerçek uygulamada ilgili SDK ile push gönderilir
};
